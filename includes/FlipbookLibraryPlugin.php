<?php
/**
 * Initialise settings for the Flipbook Library plugin
 */

class FlipbookLibraryPlugin {

    function test()
    {
        // Check if minimum requirements are installed and available
        return TRUE;
    }

    /**
     * Generate folders and establish settings
     */
    function initialise()
    {
        // Create upload folders
        //$fp = new FlipbookLibraryPlugin();
        //$fp->createFolders();
        
    }
}